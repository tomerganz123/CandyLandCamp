# Local Development Environment Variables
# Copy this file to .env.local and fill in your values

# MongoDB Connection (use your local MongoDB instance or MongoDB Atlas)
MONGODB_URI=mongodb://localhost:27017/baba-zman-local
# or for MongoDB Atlas:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/baba-zman?retryWrites=true&w=majority

# JWT Secret for admin authentication (use any secure random string)
JWT_SECRET=your-super-secret-jwt-key-for-local-development-change-this

# Next.js Configuration
NEXT_PUBLIC_SITE_URL=http://localhost:3030

# Optional: Enable detailed logging for development
NODE_ENV=development

# Optional: MongoDB connection options for local development
MONGODB_OPTIONS=retryWrites=true&w=majority

# Admin credentials (for initial setup)
# These are only used if you need to create an initial admin user
ADMIN_EMAIL=admin@babazan.local
ADMIN_PASSWORD=admin123

# Development flags
NEXT_PUBLIC_DEV_MODE=true
NEXT_PUBLIC_ENABLE_LOGGING=true
